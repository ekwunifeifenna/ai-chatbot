import {Metadata} from "next"

export const metadata: Metadata={
    title: "ai-chatbot-notes"
}
export default function NotesPage(){
    return (
        <div>Here is where the notes will go</div>
    )
}